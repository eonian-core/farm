{
  "name": "eonian",
  "private": true,
  "scripts": {
    "clean": "rimraf packages/**/lib",
    "postinstall": "git submodule update --init --recursive"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
