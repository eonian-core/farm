{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": ["packages/*"],
    "nohoist": [
      "**/ts-node",
      "**/ts-node/**",
      "**/typescript",
      "**/typescript/**",
      "**/typechain",
      "**/typechain/**",
      "**/@typechain",
      "**/@typechain/**"
    ]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "clean": "rimraf packages/**/lib",
    "graph": "nx graph",
    "start": "lerna exec --parallel -- yarn start",
    "build": "lerna run build",
    "build-contracts": "lerna run build --scope='@eonian/contracts-*'",
    "test": "lerna run test",
    "test:trace": "lerna run test:trace"
  },
  "devDependencies": {
    "lerna": "^5.1.6",
    "nx": "^14.3.6"
  }
}
